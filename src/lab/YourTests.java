package lab;

/**
 * Aufgabe H1
 * 
 * Abgabe von: <name>, <name> und <name>
 */

import static org.junit.jupiter.api.Assertions.*;

import java.util.ArrayList;

import org.junit.Assert;
import org.junit.jupiter.api.Test;

import frame.SortArray;
//import junit.framework.Assert;
import lab.Card.Suit;

/**
 * Use this class to implement your own tests.
 */
class YourTests {

	
	@Test
	void test() {
	/*ArrayList <Card> ArrayTest = new ArrayList <>();
	ArrayTest.add(new Card (0, Suit.Hearts));
	ArrayTest.add(new Card (2, Suit.Spades));
	ArrayTest.add(new Card (9, Suit.Spades));
	ArrayTest.add(new Card (0, Suit.Diamonds));
	ArrayTest.add(new Card (3, Suit.Spades));
	ArrayTest.add(new Card (15, Suit.Hearts));
	
	HybridSort sort_HybridSort = new HybridSort ();
	SortArray sortArrayTest = new SortArray (ArrayTest);
	sort_HybridSort.sort(sortArrayTest, ArrayTest.size());
	ArrayList <Card> actual_Array = new ArrayList <>(); 
	
	for (int i = 0; i < sortArrayTest.getNumberOfItems(); i++) {
		actual_Array.add(sortArrayTest.getElementAt(i));
		System.out.println (sortArrayTest.getElementAt(i));
	}
	
	System.out.println(actual_Array);
	System.out.println("/n");

	ArrayList <Card> expected_Array = new ArrayList <>();
	expected_Array.add(new Card (0, Suit.Diamonds));
	expected_Array.add(new Card (0, Suit.Hearts));
	expected_Array.add(new Card (2, Suit.Spades));
	expected_Array.add(new Card (3, Suit.Spades));
	expected_Array.add(new Card (9, Suit.Spades));
	expected_Array.add(new Card (15, Suit.Hearts));
	System.out.println(expected_Array);
	Assert.assertArrayEquals(expected_Array.toArray(), actual_Array.toArray());*/

	
	}
	
}
